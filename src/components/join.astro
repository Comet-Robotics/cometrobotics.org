---
import { discordInvite, makerspaceMap } from '../data'

interface Props {
  theme: 'light' | 'dark';
}

const { theme } = Astro.props;

let bgColor = '#D9D9D9';
let txtColor = 'black';
let borderStyle = 'none';

if (theme === 'dark') {
  bgColor = '#1e1e1e';
  txtColor = 'white';
  borderStyle = 'solid';
}
---

<div style={{ backgroundColor: bgColor, color: txtColor, padding: '30px' }}>
  <div style={{ maxWidth: '100em', margin: 'auto' }}>
    <h2>Join</h2>
    <span>
      To become a member, come to a meeting and talk to a project lead or
      officer about how you can join a team. Our general meetings are on <strong
        >Mondays at 7pm</strong
      > at the <strong>UTDesign Makerspace.</strong>
    </span>
    <div style={{ display: 'flex', padding: '15px' }}>
      <a
        href={discordInvite}
        target="_blank"
        style={{
          backgroundColor: '#1e1e1e',
          color: 'white',
          margin: '10px',
          padding: '10px',
          borderStyle: borderStyle,
          borderRadius: '10px',
          borderColor: txtColor,
        }}
      >
        <h3>Join the Discord</h3>
        <span>
          Discord is the primary method of communcaiton for the club - join for
          updates on meeting times, projects, and more.
        </span>
      </a>
      <a
        href={makerspaceMap}
        target="_blank"
        style={{
          backgroundColor: '#1e1e1e',
          color: 'white',
          margin: '10px',
          padding: '10px',
          borderStyle: borderStyle,
          borderRadius: '10px',
          borderColor: txtColor,
        }}
      >
        <h3>Find the Makerspace</h3>
        <span>
          Our meetings are hosted at the UTDesign Makerspace, located on the 2nd
          floor of the Synergy Park North building (SPN 2.220).
        </span>
      </a>
    </div>
  </div>
</div>
